{
  "name": "seimoney-monorepo",
  "version": "1.0.0",
  "description": "SeiMoney - Complete DeFi Platform for Sei Network",
  "private": true,
  "scripts": {
    "start": "./start-all.sh",
    "stop": "./stop-all.sh",
    "dev": "./start-all.sh",
    "setup": "npm run install:all && npm run build:all",
    "install:all": "npm run install:backend && npm run install:frontend",
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install",
    "build:all": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "clean": "rm -rf backend/node_modules frontend/node_modules backend/dist frontend/dist logs",
    "logs": "tail -f logs/*.log",
    "logs:backend": "tail -f logs/backend.log",
    "logs:frontend": "tail -f logs/frontend.log",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "keywords": [
    "defi",
    "sei",
    "blockchain",
    "smart-contracts",
    "payments",
    "vaults",
    "groups"
  ],
  "author": "SeiMoney Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}